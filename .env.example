# Copy this file to ".env" and fill in your key. The .env file is loaded
# automatically at startup and is gitignored so your key is never committed.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: only set this if you use a proxy / gateway. Leave it unset to talk
# to the Anthropic API directly.
# ANTHROPIC_BASE_URL=
