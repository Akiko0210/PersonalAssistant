.env
.venv/
data/
logs/
knowledge/
__pycache__/
*.pyc
.DS_Store

# Large audio assets — kept out of git (copy them across by hand). The idle/summary
# cue is optional; the app runs silently if the file is absent.
assets/*.wav
assets/*.mp3
