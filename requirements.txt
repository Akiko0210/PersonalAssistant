anthropic>=0.40
python-dotenv>=1.0
sounddevice>=0.4.6
numpy>=1.24
webrtcvad-wheels>=2.0.10
faster-whisper>=1.0
pyttsx3>=2.90
pywin32>=306
pynput>=1.7
# Windows media-session (SMTC) headset-button control; prebuilt wheels incl. 3.13
winrt-runtime>=3.2
winrt-Windows.Media>=3.2
winrt-Windows.Media.Playback>=3.2
winrt-Windows.Media.Core>=3.2
winrt-Windows.Foundation>=3.2
sentence-transformers>=2.2
chromadb>=0.5
pypdf>=4.0
# pypdf needs this to open AES-encrypted PDFs. Plenty of real trading PDFs are
# encrypted (often just to disable printing), and without it they fail to ingest
# with "cryptography>=3.1 is required for AES algorithm".
cryptography>=3.1
