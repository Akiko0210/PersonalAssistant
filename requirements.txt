anthropic>=0.40
python-dotenv>=1.0
sounddevice>=0.4.6
numpy>=1.24
webrtcvad-wheels>=2.0.10
faster-whisper>=1.0
pyttsx3>=2.90
pywin32>=306 ; sys_platform == 'win32'
pynput>=1.7
# macOS TTS (NSSpeechSynthesizer) — async speak + pause/resume, see speech/tts.py
pyobjc-framework-Cocoa>=10 ; sys_platform == 'darwin'
# Windows media-session (SMTC) headset-button control; prebuilt wheels incl. 3.13
winrt-runtime>=3.2 ; sys_platform == 'win32'
winrt-Windows.Media>=3.2 ; sys_platform == 'win32'
winrt-Windows.Media.Playback>=3.2 ; sys_platform == 'win32'
winrt-Windows.Media.Core>=3.2 ; sys_platform == 'win32'
winrt-Windows.Foundation>=3.2 ; sys_platform == 'win32'
sentence-transformers>=2.2
chromadb>=0.5
pypdf>=4.0
# pypdf needs this to open AES-encrypted PDFs. Plenty of real trading PDFs are
# encrypted (often just to disable printing), and without it they fail to ingest
# with "cryptography>=3.1 is required for AES algorithm".
cryptography>=3.1
# Trading (tastytrade REST + DXLink streaming quotes) — see TRADING_PLAN.md
requests>=2.31
websocket-client>=1.7
# Gmail tools OAuth (lib/gmail_auth.py); the API calls themselves use requests
google-auth>=2
google-auth-oauthlib>=1
